This area provides a prototype implementation of the mmuxq design, a file-system based MPMC queue with prompt, robust broadcasting "push" semantics.

The design.md file describes the conceptual design of the queue itself and the
implementation design of the prototype Basy implementation in `mmuxq`.  

Other markdown files in this directory hold historical files used during the
development.

Tests are in test/*.bats as BATS tests.
